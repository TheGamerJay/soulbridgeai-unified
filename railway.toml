[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && SECRET_KEY=railway-production-secret-key-2024 PYTHONIOENCODING=utf-8 gunicorn -c gunicorn.conf.py app:app"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"