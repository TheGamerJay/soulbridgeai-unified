[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && SECRET_KEY=railway-production-secret-key-2024 PYTHONIOENCODING=utf-8 python app_gradual.py"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"